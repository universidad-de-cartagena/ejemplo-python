apiVersion: v1
kind: Service
metadata:
  name: {{ include "ejemplo-python.fullname" . }}
  labels:
    {{- include "ejemplo-python.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ejemplo-python.selectorLabels" . | nindent 4 }}
