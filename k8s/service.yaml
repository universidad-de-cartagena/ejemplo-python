apiVersion: v1
kind: Service
metadata:
  name: ejemplo-python
  labels:
    app: ejemplo-python
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ejemplo-python
